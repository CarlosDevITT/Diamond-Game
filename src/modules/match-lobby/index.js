export class MatchLobby {
 #node=null;#match;#onStart;#game=null;
 constructor({match,onStart}){this.#match=match;this.#onStart=onStart;}
 show(game){this.#game=game;if(!this.#node){this.#node=document.createElement("section");this.#node.className="match-lobby";document.body.append(this.#node);}this.#renderHome();this.#node.hidden=false;document.body.classList.add("module-open");}
 hide(){if(this.#node)this.#node.hidden=true;document.body.classList.remove("module-open");}
 #renderHome(){this.#node.innerHTML=`<div class="match-lobby__inner"><header class="module-header"><button data-back>←</button><div><small>DIAMOND 1V1</small><strong>${this.#game.name}</strong></div></header><section class="lobby-hero"><span>MULTIPLAYER</span><h1>Desafie um amigo</h1><p>Primeira versão do fluxo 1v1. Partidas sem dinheiro real.</p></section><div class="lobby-actions"><button data-create>Criar sala</button><div class="join-box"><input data-code maxlength="6" placeholder="CÓDIGO DA SALA"><button data-join>Entrar</button></div></div></div>`;this.#node.querySelector("[data-back]").onclick=()=>this.hide();this.#node.querySelector("[data-create]").onclick=()=>this.#showRoom(this.#match.create(this.#game.id));this.#node.querySelector("[data-join]").onclick=()=>{const code=this.#node.querySelector("[data-code]").value.trim();if(code)this.#showRoom(this.#match.join(code,this.#game.id));};}
 #showRoom(room){this.#node.innerHTML=`<div class="match-lobby__inner"><header class="module-header"><button data-back>←</button><div><small>SALA 1V1</small><strong>${this.#game.name}</strong></div></header><section class="room-card"><span>CÓDIGO DA SALA</span><h1>${room.code}</h1><p>${room.players===1?"Aguardando oponente…":"2 jogadores prontos"}</p><div class="room-players"><div><b>JOGADOR A</b><small>PRONTO</small></div><div><b>JOGADOR B</b><small>${room.players===2?"PRONTO":"AGUARDANDO"}</small></div></div><button data-start ${room.players<2?"disabled":""}>Iniciar partida</button><button class="room-demo" data-demo>Simular entrada do amigo</button></section></div>`;this.#node.querySelector("[data-back]").onclick=()=>{this.#match.leave();this.#renderHome();};this.#node.querySelector("[data-demo]").onclick=()=>this.#showRoom(this.#match.join(room.code,this.#game.id));this.#node.querySelector("[data-start]").onclick=()=>{this.hide();this.#onStart(this.#game.id,{mode:"1v1",roomCode:room.code});};}
}
